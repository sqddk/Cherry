# 服务端的host
host = 127.0.0.1

# 服务端的port
port = 4000

# 时间轮每一个刻度之间的时间间隔，单位为ms
interval = 60000

# 时间轮的刻度总数
totalTicks = 10

# 时间轮的轻量级锁获取超时时间，单位为ms
wheelTimeout = 10

# 与客户端和其它服务端节点间通信的消息解析器加载包
resolverPack = cn.cherry.server.base.message.resolver
