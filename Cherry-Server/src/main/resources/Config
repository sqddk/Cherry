# 服务端的host
host = 127.0.0.1

# 服务端的port
port = 4000

# 时间轮每一个刻度之间的时间间隔，单位为ms
interval = 1000

# 时间轮的刻度总数
totalTicks = 10

# 时间轮的轻量级锁获取超时时间，单位为ns
waitTimeout = 1000

# 时间轮单个执行点可以承载的最大任务数量
taskSize = 10000

#任务执行线程池的最小线程数
minThreadNumber = 4

#任务执行线程池的最大线程数
maxThreadNumber = 8
